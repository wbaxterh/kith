/**
 * @kith/observability — placeholder.
 *
 * Real implementation lands alongside the Day 13 reliability pass. v0.1 ships
 * traces, dup-send guards, and reconnect metrics — enough to diagnose the
 * most common production failure modes (duplicate sends, silent reconnect
 * loops, delayed TTS arrivals).
 */

export {};
