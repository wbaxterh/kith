{
  "name": "@kith/voice-router",
  "version": "0.0.0",
  "description": "TTS provider router for Kith — sentence-aware chunking, pronunciation, pacing, fallback.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@kith/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
