/**
 * @kith/voice-router — placeholder.
 *
 * Day 8-9 of the v0.1 plan implements the provider router: ElevenLabs +
 * Cartesia + OpenAI TTS, pronunciation overrides, sentence-aware chunking,
 * pacing controls, fallback + dedupe playback guard.
 *
 * This is the package that directly answers the "fragment-chunking produces
 * robotic voice" failure mode from `docs/phase-1-planning.md` §1.
 */

export {};
