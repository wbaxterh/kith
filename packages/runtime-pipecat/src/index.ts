/**
 * @kith/runtime-pipecat — placeholder.
 *
 * Day 3-4 of the v0.1 plan implements the real adapter: spawns a Pipecat
 * Python sidecar (lives under `./python/`), speaks JSON-over-WebSocket,
 * emits normalized `KithEvent`s.
 *
 * See `docs/phase-1-planning.md` §4 for the full Week 1 schedule.
 */

export {};
